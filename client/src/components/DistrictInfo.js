import { useContext } from 'react';
import { StateContext } from '../contexts/State';
import { Container } from "@chakra-ui/react"

export default function DistrictInfo(props) {
    return(
        <Container>
            Hi
        </Container>
    );
}